import React from "react";
import ReactDOM from "react-dom/client";
import EncapsulatedAlignmentApp from "./EncapsulatedAlignmentApp";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <EncapsulatedAlignmentApp />
  </React.StrictMode>
);